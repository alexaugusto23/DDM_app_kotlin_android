<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.sorvetunes.sorvetunesapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".SorvetunesApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SorvetunesApp">
        <activity android:name=".AdicionaProdutoActivity"></activity>
        <activity
            android:name=".DetalheProduto"
            android:theme="@style/Theme.Toolbar"
            />
        <activity
            android:name=".PedidosActivity"
            android:theme="@style/Theme.Toolbar" />
        <activity
            android:name=".AdminActivity"
            android:theme="@style/Theme.Toolbar" />
        <activity
            android:name=".ProdutosActivity"
            android:theme="@style/Theme.Toolbar" />
        <activity
            android:name=".SobreActivity"
            android:theme="@style/Theme.Toolbar" />
        <activity android:name=".ConfigActivity" />
        <activity
            android:name=".HomeActivity"
            android:theme="@style/Theme.Toolbar" />
        <activity android:name=".DebugActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name=".MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>